{
  "name": "uterm",
  "version": "0.1.0",
  "description": "Universal Terminal — a terminal where Claude is always co-piloting",
  "type": "module",
  "bin": {
    "uterm": "dist/index.js"
  },
  "scripts": {
    "build": "node_modules/.bin/tsc",
    "dev": "node_modules/.bin/tsc --watch",
    "start": "node dist/index.js",
    "uterm": "node_modules/.bin/tsc && node dist/index.js",
    "test": "node_modules/.bin/vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.77.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
